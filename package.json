{
  "name": "mcp-local-tasks",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter=@mcp/server daemon & pnpm --filter=@mcp/server dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint || true",
    "format": "pnpm -r format || true"
  },
  "workspaces": [
    "packages/*",
    "packages/connectors/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.7.5",
    "bun-types": "^1.3.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
